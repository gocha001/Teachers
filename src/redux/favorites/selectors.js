export const selectTeachersFavorites = (state) => state.favorites.favorites;
export const selectFavoritesLastKey = (state) => state.favorites.lastKey;
export const selectFavoritesLastPage = (state) => state.favorites.lastPage;
export const selectFavoritesIsLoading = (state) => state.favorites.isLoading;
export const selectFavoritesError = (state) => state.favorites.error;
