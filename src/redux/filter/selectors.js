export const selectFilter = (state) => state.filter;
export const selectFilterTeachers = (state) => state.filter.teachers;
export const selectFilterLoading = (state) => state.filter.isLoading;
export const selectFilterError = (state) => state.filter.error;
